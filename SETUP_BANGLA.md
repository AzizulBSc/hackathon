# প্রজেক্ট সেটআপ গাইড (Bangla)

## প্রয়োজনীয় সফটওয়্যার

প্রথমে নিশ্চিত করুন যে আপনার সিস্টেমে নিচের সফটওয়্যারগুলো ইনস্টল আছে:

- PHP >= 8.1
- Composer
- Node.js >= 18
- NPM/Yarn
- MySQL/PostgreSQL

## ব্যাকএন্ড সেটআপ (Laravel)

### ১. ডাটাবেস কনফিগারেশন

```bash
cd backend
cp .env.example .env
```

`.env` ফাইলটি খুলুন এবং ডাটাবেস তথ্য আপডেট করুন:

```env
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=hackathon_db
DB_USERNAME=root
DB_PASSWORD=your_password
```

### ২. ডিপেন্ডেন্সি ইনস্টল করুন

```bash
composer install
```

### ৩. অ্যাপ্লিকেশন কি জেনারেট করুন

```bash
php artisan key:generate
```

### ৪. মাইগ্রেশন রান করুন

```bash
php artisan migrate
```

### ৫. ব্যাকএন্ড সার্ভার চালু করুন

```bash
php artisan serve
```

সার্ভার চলবে: `http://localhost:8000`

## ফ্রন্টএন্ড সেটআপ (Next.js)

### ১. ডিপেন্ডেন্সি ইনস্টল করুন

```bash
cd frontend
npm install
```

### ২. ফ্রন্টএন্ড সার্ভার চালু করুন

```bash
npm run dev
```

সার্ভার চলবে: `http://localhost:3000`

## ডেভেলপমেন্ট

দুটি টার্মিনাল খুলুন:

**টার্মিনাল ১ - ব্যাকএন্ড:**

```bash
cd backend
php artisan serve
```

**টার্মিনাল ২ - ফ্রন্টএন্ড:**

```bash
cd frontend
npm run dev
```

## কমন কমান্ডস

### Laravel (Backend)

```bash
# নতুন মডেল তৈরি করুন
php artisan make:model ModelName -m

# নতুন কন্ট্রোলার তৈরি করুন
php artisan make:controller ControllerName

# টেস্ট রান করুন
php artisan test

# ক্যাশ ক্লিয়ার করুন
php artisan cache:clear
php artisan config:clear
php artisan route:clear
```

### Next.js (Frontend)

```bash
# প্রোডাকশন বিল্ড
npm run build

# লিন্ট চেক করুন
npm run lint

# টাইপ চেক করুন
npx tsc --noEmit
```

## API টেস্টিং

ব্রাউজারে যান: `http://localhost:3000`

API স্ট্যাটাস দেখতে পারবেন হোমপেজে।

## ট্রাবলশুটিং

### CORS Error

যদি CORS error আসে, চেক করুন:

- `backend/config/cors.php` ফাইলে `allowed_origins` সেট করা আছে কিনা
- ব্যাকএন্ড সার্ভার চালু আছে কিনা

### Database Connection Error

- `.env` ফাইলে ডাটাবেস credentials চেক করুন
- MySQL/PostgreSQL সার্ভার চালু আছে কিনা দেখুন
- ডাটাবেস তৈরি করা আছে কিনা চেক করুন

### Port Already in Use

যদি পোর্ট already use করা থাকে:

```bash
# Laravel এর জন্য
php artisan serve --port=8001

# Next.js এর জন্য
npm run dev -- -p 3001
```

## প্রোডাকশন ডিপ্লয়মেন্ট

### Backend

1. এনভায়রনমেন্ট ভ্যারিয়েবল সেট করুন
2. ক্যাশ অপটিমাইজ করুন:

```bash
php artisan config:cache
php artisan route:cache
php artisan view:cache
```

### Frontend

1. বিল্ড করুন:

```bash
npm run build
```

2. Vercel/Netlify তে ডিপ্লয় করুন

## সাহায্য

কোন সমস্যা হলে README.md ফাইলটি দেখুন অথবা ডকুমেন্টেশন চেক করুন।

---

Happy Coding! 🚀
